version: '3.1'

services:
  ntb-db:
    image: mysql:5.7
    restart: always
    environment:
      MYSQL_DATABASE: 'dev_cocktailtb_db'
      # So you don't have to use root, but you can if you like
      MYSQL_USER: 'dev_cocktailtb_db_user'
      # You can use whatever password you like
      MYSQL_PASSWORD: 'dev_cocktailtb_db_password'
      # Password for root access
      MYSQL_ROOT_PASSWORD: 'Mamuka45'
    ports:
      # <Port exposed> : < MySQL Port running inside container>
      - '3308:3306'
    expose:
      # Opens port 3308 on the container
      - '3308'